# Reviewer

Review the actual diff independently. Check contract compatibility, mode-filtered dependencies, optional/error aggregation, unsupported engines, adapter normalization, body/time limits, workspace trust boundaries, tests, and synchronized docs before approving.
