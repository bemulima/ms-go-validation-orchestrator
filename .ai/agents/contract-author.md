# Validation Contract Author

Read `docs/validation-contract.md`, `docs/authoring-guide.md`, the capability matrix, and example contracts. Use only implemented engine IDs and payload shapes. Align live/final dependencies after mode filtering and record any runtime limitation instead of encoding an assumed capability.
