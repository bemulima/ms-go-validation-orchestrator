%% GENERATED — READ-ONLY PRESENTATION
%% Do not edit; source of truth is the architecture manifest.
flowchart TD
  operation["post-contracts-verify-relative — http"]
  identity["identity: http / POST / /contracts/verify"]
  operation --> identity
  access["access: internal contract authoring caller / X-Internal-Token header required when mounted below /api/v1 / service-level internal caller boundary / executes supplied verification cases and returns a receipt"]
  operation --> access
  trigger["trigger: POST route registered in the API v1 subrouter"]
  operation --> trigger
  input["input: body=verify-contract-request / Contract verification request DTO"]
  operation --> input
  business_task["business task: Execute contract verification cases and return a versioned receipt"]
  operation --> business_task
  business_process["business process: verify / Execute verification through the use case"]
  operation --> business_process
  business_rules["business rule: unknown"]
  operation --> business_rules
  implementation["implementation: router=internal/transport/http/api/v1/router.go / RegisterRoutes; handler=internal/transport/http/api/v1/handler.go / VerifyContract; use_cases=internal/usecase/contract_verification.go / VerifyContract; domain_services=unknown; repositories=unknown"]
  operation --> implementation
  data_access["data access: unknown"]
  operation --> data_access
  external_interactions["external interactions: http / configured validator engines / outbound / Verification executes cases through configured engine adapters"]
  operation --> external_interactions
  side_effects["side effects: unknown"]
  operation --> side_effects
  output["output: response=status 200 / Contract verification receipt JSON"]
  operation --> output
  errors["errors: unknown"]
  operation --> errors
  evidence["evidence: internal/transport/http/api/v1/router.go / RegisterRoutes; confidence: 1"]
  operation --> evidence
