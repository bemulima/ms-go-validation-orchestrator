schema_version: 1
name: refactor
steps:
  - state behavior and contracts that must remain unchanged
  - establish a passing focused baseline
  - change one architectural concern at a time
  - keep contract or behavior changes separate unless explicitly in scope
  - run policy, format, vet, test, and build checks
  - review the diff for accidental engine or result changes
