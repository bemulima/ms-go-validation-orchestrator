schema_version: 1
name: bugfix
steps:
  - reproduce or characterize the orchestration or adapter failure
  - identify the violated contract or invariant
  - add a focused regression test
  - implement the smallest compatible fix
  - run policy, format, vet, test, and build checks
  - synchronize owned documentation when observable behavior changed
