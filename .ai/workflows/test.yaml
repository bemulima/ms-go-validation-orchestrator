schema_version: 1
name: test
steps:
  - ./scripts/check-agent-policy.sh
  - test -z "$(gofmt -l $(git ls-files '*.go'))"
  - go vet ./...
  - go test ./... -count=1
  - go build -o /tmp/ms-go-validation-orchestrator-agent-build ./cmd/ms-go-validation-orchestrator
