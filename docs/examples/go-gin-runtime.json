{
  "version": 1,
  "kind": "workspace_contract",
  "profile": "gin-runtime",
  "profiles": ["go", "gin"],
  "workspace": {
    "required_files": ["server.go"]
  },
  "stages": [
    {
      "id": "gin-structure",
      "engine": "go.gin",
      "mode": "final",
      "targets": {
        "files": ["server.go"]
      },
      "rules": {
        "requiredRoutes": [
          { "method": "GET", "path": "/api/health", "handler": "healthHandler" }
        ]
      }
    },
    {
      "id": "gin-runtime",
      "engine": "go.gin.runtime",
      "mode": "final",
      "depends_on": ["gin-structure"],
      "targets": {
        "files": ["server.go"],
        "entrypoint": "server.go"
      },
      "checks": {
        "startupTimeoutSeconds": 20,
        "readinessPath": "/api/health",
        "requests": [
          {
            "name": "health",
            "request": { "method": "GET", "path": "/api/health" },
            "expect": { "status": 200 }
          }
        ]
      }
    }
  ],
  "links": []
}
